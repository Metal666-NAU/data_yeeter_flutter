name: data_yeeter_flutter
description: A university project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  cookie_jar: ^3.0.1
  desktop_window: ^0.4.0
  dio: ^4.0.6
  dio_cookie_manager: ^2.0.0
  file_picker: ^5.2.2
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  # flutter_webrtc: ^0.9.16
  flutter_hooks: ^0.18.5+1
  flutter_webrtc:
    git:
      url: git://github.com/flutter-webrtc/flutter-webrtc.git
  # ↑ Temporary fix until they resolve https://github.com/flutter-webrtc/flutter-webrtc/issues/1184 ↑
  go_router: ^5.1.9
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path: ^1.8.2
  path_provider: ^2.0.11
  shared_preferences: ^2.0.15
  uuid: ^3.0.6
  web_socket_channel: ^2.2.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
